# AUTHENSOR Defaults

This repository provides default community health files for repositories under the `AUTHENSOR` account (issue templates, security policy, etc.).

If `profile/README.md` is present, it is intended for an org profile page (GitHub only renders that for organizations).
